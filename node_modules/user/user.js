var well = require('../well/well.js');
var users = {};
module.exports = {
    newUser: function(userId){
        users.userId = {
            bricks: bricks,
            info: info,
            coords: coords
        };
    },
    newState: function(userId){
        well.newState();
        return well.gameState;
    }
}
