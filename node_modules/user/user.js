var users = {};
module.exports = {
    newUser: function(userId, brick, info ){
        users.userId = {
            brick: brick,
            info: info,
            coords: coords
        };
    }
}
