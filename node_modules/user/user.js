var users = {};
module.exports = {
    newUser: function(userId, brick, coords ){
        users.userId = {
            brick: brick,
            info: info,
            coords: coords
        };
    }
}
