var users = {};
module.exports = {
    newUser: function(userId, brick, info, coords ){
        users.userId = {
            brick: brick,
            info: info,
            coords: coords
        };
    }
}
